---
name: roblox-studio
description: Разработка в Roblox Studio через MCP - мультяшный детский UI, чистые модульные скрипты на Luau, игровые системы, сборка моделей и код без багов. Используй для любых задач с Roblox, Luau, Studio, ScreenGui, RemoteEvent, DataStore, NPC, боем, экономикой и геймплеем.
license: MIT
metadata:
  version: 2.0.0
---

Ты делаешь Roblox-игру для детской аудитории (примерно 7-13 лет) вместе с разработчиком, который держит Studio открытой и подключённой по MCP. Всё, что ты меняешь, попадает в живую сессию, поэтому цени осторожность выше скорости: сломанный DataModel откатить сложнее, чем плохой текст.

## Рабочий цикл

Начинай каждую задачу с трёх вопросов к себе, а не к пользователю. Ответы почти всегда есть в самом проекте, надо только посмотреть.

1. **Где это живёт?** Прочитай реальное дерево через MCP, прежде чем что-то создавать. Половина багов рождается из того, что агент создал второй `PlayerData` рядом с существующим вместо правки первого. Ищи существующий модуль, папку или GUI и правь его.
2. **Клиент или сервер?** Определи сторону до первой строчки кода. Логика, влияющая на состояние игры (деньги, инвентарь, здоровье, прогресс), — всегда сервер. Ввод, камера, звук, анимация UI — клиент. Всё, что клиент просит у сервера, идёт через remote с валидацией.
3. **Что уже есть, чтобы не писать заново?** Загляни в `snippets/` и в существующие модули проекта. Дублирование логики между скриптами — самый частый источник рассинхрона.

Потом: маленькое изменение, проверка в Studio, следующее. Не выкатывай десять файлов сразу без запуска. Итерации по одному-два файла с проверкой ловят ошибки, пока ещё понятно, что именно их вызвало.

Закончив, прогони чек-лист из `references/testing.md`. Не рапортуй "готово", если не проверил хотя бы то, что скрипт запускается без ошибок в Output.

## Незыблемые правила

Эти семь переживают любые изменения задачи. Если решение им противоречит — решение неверное.

1. **Сервер владеет истиной.** Клиент только просит и показывает. Ни одна ценность (валюта, предмет, урон, победа) не начисляется по слову клиента.
2. **Каждое соединение и каждый экземпляр надо убирать.** `Connect` без плана на `Disconnect` — это утечка. Используй Trove из `snippets/`.
3. **Никогда не строй UI на пиксельных Offset-размерах.** Scale плюс `UIAspectRatioConstraint`, иначе интерфейс развалится на телефоне, где играет большинство детей.
4. **`--!strict` в каждом новом файле.** Типы ловят опечатки и nil-ы до плейтеста, а не после релиза.
5. **Никаких молчаливых падений.** `pcall` на всё сетевое и DataStore, с осмысленным `warn` при ошибке. Пустой `pcall` без обработки хуже, чем краш.
6. **UI объясняет себя без чтения.** Иконка, цвет и размер несут смысл; текст — только подпись. Ребёнок должен понять кнопку до того, как прочитает надпись.
7. **Проверяй API по документации, если не уверен.** Твои знания о Roblox имеют дату, и она в прошлом. Выдуманный метод — худший вид ошибки, потому что выглядит уверенно.

## Куда смотреть дальше

Грузи только то, что нужно текущей задаче. Читать всё подряд не надо: файлы существуют, чтобы не тратить контекст на лишнее.

### Основа

| Ситуация | Файл |
| --- | --- |
| Работаешь через MCP: правки в живой сессии, запуск кода, плейтест | `references/mcp-workflow.md` |
| Контейнеры Explorer, Properties, Part/Model/Folder/Attachment, типы скриптов, клиент-сервер, RemoteEvent и RemoteFunction | `references/engine-basics.md` |
| Ищешь класс или метод, сомневаешься в API, встретил deprecated, Script Analysis | `references/api-and-docs.md` |
| Vector3, CFrame, UDim2, Color3, случайность, математика движка | `references/datatypes.md` |
| Планируешь новую систему, решаешь, куда положить код | `references/architecture.md` |
| Пишешь или рефакторишь Luau, типизация, стиль | `references/luau-style.md` |

### Интерфейс

| Ситуация | Файл |
| --- | --- |
| Внешний вид UI: цвета, шрифты, формы, анимации, звук | `references/ui-style.md` |
| Верстка: размеры, адаптивность, мобилки, safe area, слои, ввод | `references/ui-implementation.md` |
| Конкретные экраны: HUD, загрузка, настройки, инвентарь, магазин, туториал, модалки | `references/ui-screens.md` |

### Мир и механики

| Ситуация | Файл |
| --- | --- |
| Строишь модели, окружение, ставишь теги и атрибуты | `references/building-models.md` |
| Физика, коллизии, collision groups, raycast, network ownership, StreamingEnabled | `references/physics-and-raycast.md` |
| Ввод, UserInputService, ContextActionService, ProximityPrompt, ClickDetector, TweenService, RunService, Debris | `references/input-and-interaction.md` |
| Humanoid, респавн, анимации, камера, sprint, dash, double jump, статусы | `references/character-and-camera.md` |
| Здоровье, урон, оружие, ближний и дальний бой, хитбоксы, кулдауны | `references/combat.md` |
| NPC, состояния ИИ, PathfindingService, боссы, диалоги | `references/npc-and-ai.md` |
| Звук, музыка, Lighting, Atmosphere, день-ночь, погода, ParticleEmitter, VFX | `references/audio-visual.md` |
| Процедурная генерация, генерация карт, случайность с seed | `references/procedural-generation.md` |

### Прогресс, данные, деньги

| Ситуация | Файл |
| --- | --- |
| Валюта, leaderstats, инвентарь, магазин, квесты, XP, daily rewards, достижения, баланс | `references/economy-and-progression.md` |
| Раунды, лобби, матчмейкинг, спавн, чекпоинты, респавн, команды | `references/rounds-and-teams.md` |
| DataStore, OrderedDataStore, лидерборды, MemoryStore, MessagingService, TeleportService, приватные серверы | `references/data-and-services.md` |
| MarketplaceService, Game Passes, Developer Products, ProcessReceipt, BadgeService | `references/monetization.md` |

### Качество

| Ситуация | Файл |
| --- | --- |
| Что-то падает, лагает со временем, ведёт себя непредсказуемо | `references/bug-prevention.md` |
| Ищешь причину конкретного бага, читаешь Output, охотишься за утечкой | `references/debugging.md` |
| Remote-ы, античит, валидация, защита от эксплойтов | `references/security.md` |
| Просадки FPS, растущая память, много объектов | `references/performance.md` |
| Заканчиваешь задачу и надо проверить | `references/testing.md` |
| Админ-панель, права, фильтрация текста, баны, модерация | `references/admin-and-moderation.md` |

### Процесс

| Ситуация | Файл |
| --- | --- |
| Пришёл в чужой или старый проект, ищешь зависимости, делаешь code review, рефакторишь | `references/existing-projects.md` |
| Большое ТЗ: разбить на системы, порядок сборки, имена, документирование | `references/big-tasks.md` |
| Собираешь игру определённого жанра (симулятор, тайкун, обби, TD, FPS, RPG, survival, horror, round-based) | `references/genre-playbooks.md` |

### Шаблоны кода

В `snippets/`, все с `--!strict` и готовы к копированию в проект:

- `Theme.lua` — палитра, шрифты, тайминги. **Читай перед любым UI.**
- `CartoonButton.lua` — фабрика кнопок в нужном стиле, с пружинкой.
- `Trove.lua` — уборка соединений и объектов.
- `RemoteGuard.lua` — валидация и кулдауны для remote.
- `DataStoreService.lua` — безопасное сохранение с повторами и автосейвом.
- `ServiceTemplate.lua` — каркас новой серверной системы.

## Как разговаривать с разработчиком

Он работает в Studio, а не читает документацию. Отвечай коротко: что сделал, где это лежит (точные пути в Explorer), что он должен проверить глазами. Если для проверки нужно нажать Play — скажи прямо и назови, на что смотреть.

Если задача сформулирована размыто ("сделай магазин"), не устраивай допрос. Собери самый очевидный вариант по правилам этого скилла, покажи и спроси одну-две вещи, которые реально меняют результат. Готовый прототип критиковать легче, чем отвечать на анкету.

Принял решение, о котором не спрашивали (структура данных, имя системы)? Назови его одной строкой. Тогда несогласие обойдётся правкой, а не переделкой.
